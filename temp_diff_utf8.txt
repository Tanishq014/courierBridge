diff --git a/app/main.py b/app/main.py
index 08b0886..5e6c60b 100644
--- a/app/main.py
+++ b/app/main.py
@@ -119,9 +119,10 @@ def ensure_lightweight_migrations():
                 columns = {column["name"] for column in inspector.get_columns("shipments")}
                 if "row_color" not in columns:
                     connection.execute(text("ALTER TABLE shipments ADD COLUMN row_color VARCHAR"))
-    except Exception:
-        # Keep startup available even if a non-SQLite database handles migrations externally.
-        pass
+                if "custom_duty" not in columns:
+                    connection.execute(text("ALTER TABLE shipments ADD COLUMN custom_duty BOOLEAN DEFAULT false"))
+    except Exception as exc:
+        raise RuntimeError("Database lightweight migration failed") from exc
 
 
 ensure_lightweight_migrations()
diff --git a/app/models.py b/app/models.py
index ec1c380..b9a2cbb 100644
--- a/app/models.py
+++ b/app/models.py
@@ -58,7 +58,8 @@ class Shipment(Base):
     
     # 7. Status & Tracking 
     status_raw_text = Column(String)
-    overall_status = Column(String, index=True, default="booked") # booked/received/in_transit/customs/out_for_delivery/delivered/rto/return_damage/exception/unknown
+    custom_duty = Column(Boolean, default=False)
+    overall_status = Column(String, index=True, default="booked") # booked/packed/bagged/in_scan/bagging/received/in_transit/customs/hand_over_to_airline/at_destination/custom_clearance/at_lm_partner/out_for_delivery/delivered/rto/return_damage/exception/unknown
     row_color = Column(String, nullable=True) # manual row highlight: green/yellow/red; blank uses status default
     requires_lm_awb = Column(Boolean, default=False)
     
diff --git a/app/routes/dashboard.py b/app/routes/dashboard.py
index 13a806f..d894b7a 100644
--- a/app/routes/dashboard.py
+++ b/app/routes/dashboard.py
@@ -8,7 +8,13 @@ from datetime import datetime, timezone
 router = APIRouter()
 templates = Jinja2Templates(directory="app/templates")
 
+from fastapi.responses import RedirectResponse
+
 @router.get("/")
+def home():
+    return RedirectResponse(url="/shipments", status_code=303)
+
+@router.get("/dashboard")
 def dashboard(request: Request, db: Session = Depends(get_db)):
     active_shipments = db.query(Shipment).filter(Shipment.overall_status.notin_(["delivered", "rto", "return_damage"])).all()
     
diff --git a/app/routes/shipments.py b/app/routes/shipments.py
index 1dc4e14..e083a98 100644
--- a/app/routes/shipments.py
+++ b/app/routes/shipments.py
@@ -91,6 +91,12 @@ def get_courier_options(db: Session) -> list[str]:
     tracking_couriers = [row[0] for row in db.query(TrackingNumber.courier_name).distinct().all() if row[0]]
     return sorted({c.strip() for c in DEFAULT_COURIERS + shipment_couriers + tracking_couriers if c and c.strip()}, key=str.lower)
 
+
+def get_service_options(db: Session) -> list[str]:
+    vendor_partners = [row[0] for row in db.query(Shipment.vendor_partner).distinct().all() if row[0]]
+    shipment_couriers = [row[0] for row in db.query(Shipment.courier_company).distinct().all() if row[0]]
+    return sorted({s.strip() for s in vendor_partners + shipment_couriers if s and s.strip()}, key=str.lower)
+
 def parse_receiver_address(raw_notes: str | None) -> dict[str, str]:
     blank = {
         "line_1": "",
@@ -299,20 +305,20 @@ def upsert_tracking(db: Session, shipment_id: int, t_type: str, number: str, cou
     number = number.strip() if number else ""
     if not number:
         return
-        
+
     if t_type == "main_awb":
         is_primary = True
     elif t_type == "lm_awb":
         is_primary = False
-        
+
     if is_primary:
         db.query(TrackingNumber).filter(TrackingNumber.shipment_id == shipment_id).update({"is_primary": False})
-        
+
     tn = db.query(TrackingNumber).filter(
         TrackingNumber.shipment_id == shipment_id,
         TrackingNumber.tracking_type == t_type
     ).first()
-    
+
     if tn:
         tn.tracking_number = number
         if courier:
@@ -330,14 +336,16 @@ def upsert_tracking(db: Session, shipment_id: int, t_type: str, number: str, cou
 
 @router.get("")
 def list_shipments(
-    request: Request, 
+    request: Request,
     db: Session = Depends(get_db),
     q: str = "",
     status: str = "",
-    country: str = ""
+    country: str = "",
+    custom_duty: str = "",
+    service: str = ""
 ):
     query = db.query(Shipment).outerjoin(TrackingNumber)
-    
+
     if q:
         query = query.filter(
             or_(
@@ -349,7 +357,9 @@ def list_shipments(
                 TrackingNumber.tracking_number.ilike(f"%{q}%"),
                 Shipment.courier_company.ilike(f"%{q}%"),
                 Shipment.vendor_partner.ilike(f"%{q}%"),
-                Shipment.status_raw_text.ilike(f"%{q}%")
+                Shipment.status_raw_text.ilike(f"%{q}%"),
+                Shipment.internal_notes.ilike(f"%{q}%"),
+                Shipment.customer_notes.ilike(f"%{q}%")
             )
         )
     if status:
@@ -357,7 +367,15 @@ def list_shipments(
     normalized_country_filter = normalize_country(country)
     if normalized_country_filter:
         query = query.filter(Shipment.destination_country == normalized_country_filter)
-        
+    if custom_duty == "yes":
+        query = query.filter(Shipment.custom_duty == True)
+    elif custom_duty == "no":
+        query = query.filter(Shipment.custom_duty == False)
+    if service:
+        query = query.filter(or_(
+            Shipment.vendor_partner == service,
+            Shipment.courier_company == service
+        ))
     shipments = query.order_by(Shipment.booking_date.desc()).distinct().all()
     shipment_previews = {}
     for shipment in shipments:
@@ -368,17 +386,21 @@ def list_shipments(
             "address": format_receiver_address(receiver_address),
         }
     courier_options = get_courier_options(db)
+    service_options = get_service_options(db)
     country_options = get_country_options(db)
-    
+
     return templates.TemplateResponse("shipments/list.html", {
         "request": request,
         "shipments": shipments,
         "shipment_previews": shipment_previews,
         "courier_options": courier_options,
+        "service_options": service_options,
         "country_options": country_options,
         "q": q,
         "status": status,
-        "country": normalized_country_filter
+        "country": normalized_country_filter,
+        "custom_duty": custom_duty,
+        "service": service,
     })
 
 @router.get("/new")
@@ -389,6 +411,7 @@ def new_shipment_form(request: Request, db: Session = Depends(get_db)):
         "today": today,
         "item_raw_text": "",
         "courier_options": get_courier_options(db),
+        "service_options": get_service_options(db),
         "country_options": get_country_options(db)
     })
 
@@ -408,9 +431,9 @@ def create_shipment(
     customer_phone: str = Form(""),
     name_country_raw: str = Form(""),
     contact_or_reference_raw: str = Form(""),
-    
+
     item_raw_text: str = Form(""),
-    
+
     dead_weight: str = Form("0.0"),
     volumetric_weight: str = Form("0.0"),
     volumetric_length: str = Form(""),
@@ -419,7 +442,7 @@ def create_shipment(
     weight_basis: str = Form("actual"),
     dead_weight_unit: str = Form("KG"),
     volumetric_weight_unit: str = Form("KG"),
-    
+
     customer_rate_text: str = Form(""),
     customer_charged_weight: str = Form(""),
     customer_charged_weight_unit: str = Form("KG"),
@@ -428,40 +451,41 @@ def create_shipment(
     vendor_charged_weight_unit: str = Form("KG"),
     courier_company: str = Form(""),
     vendor_partner: str = Form(""),
-    
+
     promised_days_text: str = Form(""),
     promised_days_number: str = Form(""),
-    
+
     billed_amount: str = Form("0.0"),
     received_amount: str = Form("0.0"),
     self_cost: str = Form("0.0"),
     other_expense: str = Form("0.0"),
-    
+
     status_raw_text: str = Form(""),
     overall_status: str = Form("booked"),
     requires_lm_awb: bool = Form(False),
-    
+    custom_duty: bool = Form(False),
+
     booking_date: str = Form(""),
     main_tracking_number: str = Form(""),
     main_tracking_courier: str = Form(""),
     lm_awb_number: str = Form(""),
     lm_awb_courier: str = Form(""),
-    
+
     internal_notes: str = Form(""),
     customer_notes: str = Form(""),
     balance_notes: str = Form(""),
     raw_excel_notes: str = Form(""),
     raw_excel_row_text: str = Form("")
 ):
-    parsed_billed = calculate_rate_amount(customer_charged_weight, customer_rate_text) or parse_decimal(billed_amount)
+    parsed_billed = parse_decimal(billed_amount) or calculate_rate_amount(customer_charged_weight, customer_rate_text)
     parsed_received = parse_decimal(received_amount)
-    parsed_self_cost = calculate_rate_amount(vendor_charged_weight, vendor_rate_text) or parse_decimal(self_cost)
+    parsed_self_cost = parse_decimal(self_cost) or calculate_rate_amount(vendor_charged_weight, vendor_rate_text)
     parsed_other_exp = parse_decimal(other_expense)
-    
+
     total_cost = parsed_self_cost + parsed_other_exp
     service_value = parsed_billed - total_cost
     balance_amount = parsed_billed - parsed_received
-    
+
     parsed_booking_date = now_ist()
     if booking_date and booking_date.strip():
         try:
@@ -473,6 +497,13 @@ def create_shipment(
     normalized_receiver_state = normalize_proper_case(receiver_state)
     normalized_receiver_zip = " ".join((receiver_zip or "").strip().split()).upper()
     normalized_vendor_partner = normalize_proper_case(vendor_partner)
+
+    customer_name = normalize_proper_case(customer_name)
+    receiver_name = normalize_proper_case(receiver_name)
+    receiver_address_line_1 = normalize_proper_case(receiver_address_line_1)
+    receiver_address_line_2 = normalize_proper_case(receiver_address_line_2)
+    receiver_address_line_3 = normalize_proper_case(receiver_address_line_3)
+    status_raw_text = normalize_proper_case(status_raw_text)
     dead_weight_value = parse_float(dead_weight)
     volumetric_weight_value = calculate_volumetric_weight(volumetric_length, volumetric_width, volumetric_height) or parse_float(volumetric_weight)
     weight_basis_value = "volumetric" if volumetric_weight_value > dead_weight_value else "actual"
@@ -533,6 +564,7 @@ def create_shipment(
         status_raw_text=status_raw_text,
         overall_status=overall_status,
         requires_lm_awb=requires_lm_awb,
+        custom_duty=custom_duty,
         internal_notes=internal_notes,
         customer_notes=customer_notes,
         balance_notes=balance_notes,
@@ -540,27 +572,27 @@ def create_shipment(
         raw_excel_row_text=raw_excel_row_text,
         last_status_text=status_raw_text
     )
-    
+
     if status_raw_text and status_raw_text.strip():
         shipment.last_status_text = status_raw_text
         shipment.last_status_at = parsed_booking_date
-    
+
     if overall_status == "delivered":
         shipment.delivered_at = parsed_booking_date
-        
+
     db.add(shipment)
     db.commit()
     db.refresh(shipment)
 
     if overall_status or status_raw_text.strip():
         add_status_timeline_event(db, shipment, overall_status or "booked", status_raw_text, "shipment_create")
-    
+
     # Tracking numbers
     upsert_tracking(db, shipment.id, "main_awb", main_tracking_number, main_tracking_courier, True)
     upsert_tracking(db, shipment.id, "lm_awb", lm_awb_number, lm_awb_courier, False)
     db.commit()
-    
-    return RedirectResponse(url=f"/shipments/{shipment.id}", status_code=303)
+
+    return RedirectResponse(url="/shipments", status_code=303)
 
 @router.post("/{shipment_id}/quick-update")
 def quick_update_shipment(
@@ -604,7 +636,7 @@ def shipment_detail(request: Request, shipment_id: int, db: Session = Depends(ge
     shipment = db.query(Shipment).filter(Shipment.id == shipment_id).first()
     if not shipment:
         return RedirectResponse(url="/shipments", status_code=303)
-        
+
     receiver_address = parse_receiver_address(shipment.raw_excel_notes)
     item_raw_text = parse_item_raw_text(shipment.raw_excel_notes)
     rate_details = parse_rate_details(shipment.raw_excel_notes)
@@ -626,10 +658,10 @@ def edit_shipment_form(request: Request, shipment_id: int, db: Session = Depends
     shipment = db.query(Shipment).filter(Shipment.id == shipment_id).first()
     if not shipment:
         return RedirectResponse(url="/shipments", status_code=303)
-        
+
     main_awb = next((tn for tn in shipment.tracking_numbers if tn.tracking_type == "main_awb"), None)
     lm_awb = next((tn for tn in shipment.tracking_numbers if tn.tracking_type == "lm_awb"), None)
-        
+
     receiver_address = parse_receiver_address(shipment.raw_excel_notes)
     item_raw_text = parse_item_raw_text(shipment.raw_excel_notes)
     rate_details = parse_rate_details(shipment.raw_excel_notes)
@@ -646,6 +678,7 @@ def edit_shipment_form(request: Request, shipment_id: int, db: Session = Depends
         "customer_per_kg_rate": extract_per_kg_rate(shipment.customer_rate_text),
         "vendor_per_kg_rate": extract_per_kg_rate(shipment.vendor_rate_text),
         "courier_options": get_courier_options(db),
+        "service_options": get_service_options(db),
         "country_options": get_country_options(db)
     })
 
@@ -666,9 +699,9 @@ def update_shipment(
     customer_phone: str = Form(""),
     name_country_raw: str = Form(""),
     contact_or_reference_raw: str = Form(""),
-    
+
     item_raw_text: str = Form(""),
-    
+
     dead_weight: str = Form("0.0"),
     volumetric_weight: str = Form("0.0"),
     volumetric_length: str = Form(""),
@@ -677,7 +710,7 @@ def update_shipment(
     weight_basis: str = Form("actual"),
     dead_weight_unit: str = Form("KG"),
     volumetric_weight_unit: str = Form("KG"),
-    
+
     customer_rate_text: str = Form(""),
     customer_charged_weight: str = Form(""),
     customer_charged_weight_unit: str = Form("KG"),
@@ -686,25 +719,26 @@ def update_shipment(
     vendor_charged_weight_unit: str = Form("KG"),
     courier_company: str = Form(""),
     vendor_partner: str = Form(""),
-    
+
     promised_days_text: str = Form(""),
     promised_days_number: str = Form(""),
-    
+
     billed_amount: str = Form("0.0"),
     received_amount: str = Form("0.0"),
     self_cost: str = Form("0.0"),
     other_expense: str = Form("0.0"),
-    
+
     status_raw_text: str = Form(""),
     overall_status: str = Form("booked"),
     requires_lm_awb: bool = Form(False),
-    
+    custom_duty: bool = Form(False),
+
     booking_date: str = Form(""),
     main_tracking_number: str = Form(""),
     main_tracking_courier: str = Form(""),
     lm_awb_number: str = Form(""),
     lm_awb_courier: str = Form(""),
-    
+
     internal_notes: str = Form(""),
     customer_notes: str = Form(""),
     balance_notes: str = Form(""),
@@ -717,26 +751,33 @@ def update_shipment(
 
     old_status = shipment.overall_status or "booked"
     old_notes = shipment.status_raw_text or ""
-        
-    parsed_billed = calculate_rate_amount(customer_charged_weight, customer_rate_text) or parse_decimal(billed_amount)
+
+    parsed_billed = parse_decimal(billed_amount) or calculate_rate_amount(customer_charged_weight, customer_rate_text)
     parsed_received = parse_decimal(received_amount)
-    parsed_self_cost = calculate_rate_amount(vendor_charged_weight, vendor_rate_text) or parse_decimal(self_cost)
+    parsed_self_cost = parse_decimal(self_cost) or calculate_rate_amount(vendor_charged_weight, vendor_rate_text)
     parsed_other_exp = parse_decimal(other_expense)
-        
+
     total_cost = parsed_self_cost + parsed_other_exp
     service_value = parsed_billed - total_cost
     balance_amount = parsed_billed - parsed_received
-    
+
     if booking_date and booking_date.strip():
         try:
             shipment.booking_date = datetime.strptime(booking_date.strip(), "%Y-%m-%d")
         except ValueError:
             pass
-    
+
     normalized_destination_city = normalize_proper_case(destination_city)
     normalized_receiver_state = normalize_proper_case(receiver_state)
     normalized_receiver_zip = " ".join((receiver_zip or "").strip().split()).upper()
     normalized_vendor_partner = normalize_proper_case(vendor_partner)
+
+    customer_name = normalize_proper_case(customer_name)
+    receiver_name = normalize_proper_case(receiver_name)
+    receiver_address_line_1 = normalize_proper_case(receiver_address_line_1)
+    receiver_address_line_2 = normalize_proper_case(receiver_address_line_2)
+    receiver_address_line_3 = normalize_proper_case(receiver_address_line_3)
+    status_raw_text = normalize_proper_case(status_raw_text)
     dead_weight_value = parse_float(dead_weight)
     volumetric_weight_value = calculate_volumetric_weight(volumetric_length, volumetric_width, volumetric_height) or parse_float(volumetric_weight)
     weight_basis_value = "volumetric" if volumetric_weight_value > dead_weight_value else "actual"
@@ -757,9 +798,9 @@ def update_shipment(
     shipment.customer_phone = customer_phone
     shipment.name_country_raw = name_country_raw
     shipment.contact_or_reference_raw = contact_or_reference_raw
-    
+
     shipment.parcel_description = ""
-    
+
     shipment.dead_weight = dead_weight_value
     shipment.volumetric_weight = volumetric_weight_value
     shipment.charged_weight = 0.0
@@ -767,15 +808,15 @@ def update_shipment(
     shipment.dead_weight_text = "KG"
     shipment.volumetric_weight_text = "KG"
     shipment.charged_weight_text = ""
-    
+
     shipment.customer_rate_text = customer_rate_text
     shipment.vendor_rate_text = vendor_rate_text
     shipment.courier_company = main_tracking_courier
     shipment.vendor_partner = normalized_vendor_partner
-    
+
     shipment.promised_days_text = promised_days_text
     shipment.promised_days_number = parse_int(promised_days_number)
-    
+
     shipment.billed_amount = parsed_billed
     shipment.received_amount = parsed_received
     shipment.self_cost = parsed_self_cost
@@ -783,7 +824,7 @@ def update_shipment(
     shipment.total_cost = total_cost
     shipment.service_value = service_value
     shipment.balance_amount = balance_amount
-    
+
     new_status = overall_status.strip() or old_status
     new_notes = status_raw_text.strip()
     shipment.status_raw_text = new_notes
@@ -791,7 +832,8 @@ def update_shipment(
     if new_status != old_status or new_notes != old_notes:
         add_status_timeline_event(db, shipment, new_status, new_notes, "shipment_edit")
     shipment.requires_lm_awb = requires_lm_awb
-    
+    shipment.custom_duty = custom_duty
+
     shipment.internal_notes = internal_notes
     shipment.customer_notes = customer_notes
     shipment.balance_notes = balance_notes
@@ -809,9 +851,9 @@ def update_shipment(
         "vendor_charged_weight_unit": vendor_charged_weight_unit,
     })
     shipment.raw_excel_row_text = raw_excel_row_text
-    
+
     upsert_tracking(db, shipment.id, "main_awb", main_tracking_number, main_tracking_courier, True)
     upsert_tracking(db, shipment.id, "lm_awb", lm_awb_number, lm_awb_courier, False)
-    
+
     db.commit()
-    return RedirectResponse(url=f"/shipments/{shipment.id}", status_code=303)
+    return RedirectResponse(url="/shipments", status_code=303)
diff --git a/app/static/css/style.css b/app/static/css/style.css
index 50995ab..8f83ee6 100644
--- a/app/static/css/style.css
+++ b/app/static/css/style.css
@@ -1887,4 +1887,9 @@ body.sidebar-collapsed.sidebar-hovering .sidebar-toggle {
     border: 1px solid var(--line);
     border-radius: 12px;
     background: #fff;
-}
\ No newline at end of file
+}
+/* Capitalize text fields while typing */
+.shipment-form input[type="text"]:not([name="main_tracking_number"]):not([name="lm_awb_number"]):not(.country-normalized-input),
+.shipment-form textarea {
+    text-transform: capitalize;
+}
diff --git a/app/templates/base.html b/app/templates/base.html
index 246afbd..8258c65 100644
--- a/app/templates/base.html
+++ b/app/templates/base.html
@@ -25,7 +25,7 @@
 
             <div class="nav-label">Workspace</div>
             <nav class="nav-list" aria-label="Primary navigation">
-                <a href="/" class="nav-link {% if path == '/' %}active{% endif %}">
+                <a href="/dashboard" class="nav-link {% if path == '/dashboard' %}active{% endif %}">
                     Dashboard
                 </a>
                 <a href="/shipments" class="nav-link {% if path.startswith('/shipments') and path != '/shipments/new' %}active{% endif %}">
diff --git a/app/templates/customers/detail.html b/app/templates/customers/detail.html
index f18cc05..a73e073 100644
--- a/app/templates/customers/detail.html
+++ b/app/templates/customers/detail.html
@@ -61,7 +61,7 @@
                         <span class="badge
                             {% if s.overall_status == 'delivered' %}badge-success
                             {% elif s.overall_status == 'exception' %}badge-danger
-                            {% elif s.overall_status == 'customs' %}badge-warning
+                            {% elif s.overall_status in ['customs', 'custom_clearance'] %}badge-warning
                             {% else %}badge-neutral{% endif %}">
                             {{ s.overall_status | capitalize | replace('_', ' ') }}
                         </span>
diff --git a/app/templates/dashboard.html b/app/templates/dashboard.html
index ec1bc65..65ccdbb 100644
--- a/app/templates/dashboard.html
+++ b/app/templates/dashboard.html
@@ -73,7 +73,7 @@
                     <td>
                         <span class="badge
                             {% if s.overall_status == 'exception' %}badge-danger
-                            {% elif s.overall_status == 'customs' %}badge-warning
+                            {% elif s.overall_status in ['customs', 'custom_clearance'] %}badge-warning
                             {% else %}badge-neutral{% endif %}">
                             {{ s.overall_status | capitalize | replace('_', ' ') }}
                         </span>
diff --git a/app/templates/shipments/detail.html b/app/templates/shipments/detail.html
index 44275e5..bd32941 100644
--- a/app/templates/shipments/detail.html
+++ b/app/templates/shipments/detail.html
@@ -15,8 +15,8 @@
         <span class="badge
             {% if shipment.overall_status == 'delivered' %}badge-success
             {% elif shipment.overall_status in ['exception', 'rto', 'return_damage', 'undelivered'] %}badge-danger
-            {% elif shipment.overall_status == 'customs' %}badge-warning
-            {% elif shipment.overall_status in ['in_transit', 'out_for_delivery', 'at_lm_partner', 'bagged'] %}badge-info
+            {% elif shipment.overall_status in ['customs', 'custom_clearance'] %}badge-warning
+            {% elif shipment.overall_status in ['in_transit', 'out_for_delivery', 'at_lm_partner', 'bagged', 'bagging', 'in_scan', 'hand_over_to_airline', 'at_destination'] %}badge-info
             {% else %}badge-neutral{% endif %}">
             {{ shipment.overall_status | capitalize | replace('_', ' ') }}
         </span>
@@ -42,7 +42,30 @@
         </div>
         <div class="detail-list">
             <div class="detail-item"><span class="detail-label">Sender</span><span class="detail-value">{{ shipment.customer_name }}</span></div>
-            <div class="detail-item"><span class="detail-label">Sender phone</span><span class="detail-value">{{ shipment.customer_phone or 'Not provided' }}</span></div>
+            <div class="detail-item"><span class="detail-label">Sender phone</span><span class="detail-value">
+                {% if shipment.customer_phone %}
+                    {% set primary_tn = namespace(item=none) %}
+                    {% for tn in shipment.tracking_numbers %}
+                        {% if tn.is_primary %}{% set primary_tn.item = tn %}{% endif %}
+                    {% endfor %}
+                    {% if not primary_tn.item and shipment.tracking_numbers %}{% set primary_tn.item = shipment.tracking_numbers[0] %}{% endif %}
+
+                    {% set primary_tn_url = '' %}
+                    {% if primary_tn.item %}
+                        {% set effective_tn_courier = primary_tn.item.courier_name or (shipment.courier_company if primary_tn.item.tracking_type == 'main_awb' else '') %}
+                        {% set tn_url = tracking_url(effective_tn_courier, primary_tn.item.tracking_number, tracking_type=primary_tn.item.tracking_type) %}
+                        {% set tn_site_url = tracking_site_url(effective_tn_courier, primary_tn.item.tracking_number) %}
+                        {% set primary_tn_url = tn_url or tn_site_url or '' %}
+                    {% endif %}
+
+                    {% set wa_message = "Here is your tracking link: " ~ primary_tn_url ~ " and AWB: " ~ (primary_tn.item.tracking_number if primary_tn.item else '') %}
+                    <a href="#" class="whatsapp-app-link" data-whatsapp-phone="{{ shipment.customer_phone }}" data-whatsapp-message="{{ wa_message }}" style="text-decoration: underline; color: inherit;">
+                        {{ shipment.customer_phone }}
+                    </a>
+                {% else %}
+                    Not provided
+                {% endif %}
+            </span></div>
             <div class="detail-item"><span class="detail-label">Receiver</span><span class="detail-value">{{ shipment.receiver_name or 'Same as customer' }}</span></div>
             <div class="detail-item"><span class="detail-label">Receiver phone</span><span class="detail-value">{{ shipment.contact_or_reference_raw or 'Not provided' }}</span></div>
             <div class="detail-item wide"><span class="detail-label">Receiver address</span><span class="detail-value">{{ receiver_address_text or 'Not provided' }}</span></div>
@@ -160,13 +183,44 @@
     <div class="dialog-body"><textarea autocomplete="new-password" spellcheck="false" id="waText" rows="10"></textarea></div>
     <div class="dialog-footer">
         <button onclick="document.getElementById('whatsappModal').close()" class="btn btn-secondary">Close</button>
-        <button onclick="copyWA()" class="btn btn-primary">Copy update</button>
+        <button onclick="copyWA()" class="btn btn-secondary">Copy update</button>
+        <button onclick="openGeneratedWhatsApp()" class="btn btn-primary">Open WhatsApp</button>
     </div>
 </dialog>
 {% endblock %}
 
 {% block scripts %}
 <script>
+    const shipmentWhatsappPhone = {{ (shipment.customer_phone or '') | tojson }};
+
+    function normalizeWhatsappPhone(phone) {
+        let digits = String(phone || '').replace(/\D/g, '');
+        if (digits.length === 10) digits = '91' + digits;
+        return digits;
+    }
+
+    function openWhatsAppApp(phone, message) {
+        const normalizedPhone = normalizeWhatsappPhone(phone);
+        if (!normalizedPhone) return false;
+        const encodedMessage = encodeURIComponent(message || '');
+        const appUrl = `whatsapp://send?phone=${normalizedPhone}&text=${encodedMessage}`;
+        const webUrl = `https://wa.me/${normalizedPhone}?text=${encodedMessage}`;
+        let fallbackTimer = null;
+        const cancelFallback = () => {
+            if (fallbackTimer) clearTimeout(fallbackTimer);
+            fallbackTimer = null;
+        };
+        window.addEventListener('blur', cancelFallback, { once: true });
+        document.addEventListener('visibilitychange', () => {
+            if (document.hidden) cancelFallback();
+        }, { once: true });
+        fallbackTimer = setTimeout(() => {
+            window.open(webUrl, '_blank', 'noopener');
+        }, 1200);
+        window.location.href = appUrl;
+        return true;
+    }
+
     function generateWhatsApp() {
         const customer = {{ (shipment.customer_name or shipment.name_country_raw or 'Customer') | tojson }};
         const dest = {{ shipment.destination_country | tojson }};
@@ -185,12 +239,25 @@
         document.getElementById('whatsappModal').showModal();
     }
 
+    function openGeneratedWhatsApp() {
+        const text = document.getElementById('waText').value;
+        openWhatsAppApp(shipmentWhatsappPhone, text);
+        document.getElementById('whatsappModal').close();
+    }
+
     async function copyWA() {
         const text = document.getElementById('waText').value;
         await navigator.clipboard.writeText(text);
         document.getElementById('whatsappModal').close();
     }
 
+    document.querySelectorAll('.whatsapp-app-link').forEach(link => {
+        link.addEventListener('click', event => {
+            event.preventDefault();
+            openWhatsAppApp(link.dataset.whatsappPhone, link.dataset.whatsappMessage || '');
+        });
+    });
+
     document.querySelectorAll('.copy-tracking-link').forEach(link => {
         link.addEventListener('click', event => {
             if (link.dataset.copyMode !== 'true' || !link.dataset.copyNumber) return;
diff --git a/app/templates/shipments/edit.html b/app/templates/shipments/edit.html
index 70a9ab0..d046a10 100644
--- a/app/templates/shipments/edit.html
+++ b/app/templates/shipments/edit.html
@@ -14,7 +14,7 @@
 
 <div class="card form-card">
     <form method="POST" action="/shipments/{{ shipment.id }}/edit" class="shipment-form compact-shipment-form">
-        
+
         <div class="form-section-title">
             <h3>Sender & Receiver</h3>
             <div class="form-group inline-date">
@@ -125,7 +125,7 @@
                 <input autocomplete="new-password" spellcheck="false" type="text" name="vendor_partner" value="{{ shipment.vendor_partner or '' }}" placeholder="e.g. Nikhil, Self">
             </div>
             <div class="form-group">
-                <label>Main Tracking Courier <span class="required">*</span></label>
+                <label>First Courier Company <span class="required">*</span></label>
                 <select name="main_tracking_courier" id="main_tracking_courier" required>
                     {% set current_main_courier = main_awb.courier_name if main_awb and main_awb.courier_name else shipment.courier_company %}
                     <option value="">Select courier</option>
@@ -134,7 +134,7 @@
                 </select>
             </div>
             <div class="form-group">
-                <label>Main Tracking Number</label>
+                <label>First Courier Number</label>
                 <input autocomplete="new-password" spellcheck="false" type="text" name="main_tracking_number" value="{{ main_awb.tracking_number if main_awb else '' }}">
             </div>
             <div class="form-group">
@@ -156,9 +156,14 @@
                     <option value="booked" {% if shipment.overall_status == 'booked' %}selected{% endif %}>Booked</option>
                     <option value="packed" {% if shipment.overall_status == 'packed' %}selected{% endif %}>Packed</option>
                     <option value="bagged" {% if shipment.overall_status == 'bagged' %}selected{% endif %}>Bagged</option>
+                    <option value="in_scan" {% if shipment.overall_status == 'in_scan' %}selected{% endif %}>In Scan</option>
+                    <option value="bagging" {% if shipment.overall_status == 'bagging' %}selected{% endif %}>Bagging</option>
                     <option value="received" {% if shipment.overall_status == 'received' %}selected{% endif %}>Received</option>
                     <option value="in_transit" {% if shipment.overall_status == 'in_transit' %}selected{% endif %}>In Transit</option>
                     <option value="customs" {% if shipment.overall_status == 'customs' %}selected{% endif %}>Customs</option>
+                    <option value="hand_over_to_airline" {% if shipment.overall_status == 'hand_over_to_airline' %}selected{% endif %}>Hand over to airline</option>
+                    <option value="at_destination" {% if shipment.overall_status == 'at_destination' %}selected{% endif %}>At destination</option>
+                    <option value="custom_clearance" {% if shipment.overall_status == 'custom_clearance' %}selected{% endif %}>Custom Clearance</option>
                     <option value="at_lm_partner" {% if shipment.overall_status == 'at_lm_partner' %}selected{% endif %}>At LM Partner</option>
                     <option value="out_for_delivery" {% if shipment.overall_status == 'out_for_delivery' %}selected{% endif %}>Out for Delivery</option>
                     <option value="undelivered" {% if shipment.overall_status == 'undelivered' %}selected{% endif %}>Undelivered</option>
@@ -180,9 +185,15 @@
                 <input autocomplete="new-password" spellcheck="false" type="text" name="status_raw_text" value="{{ shipment.status_raw_text or '' }}" placeholder="e.g. Customer asked to hold, pending customs, follow-up needed">
             </div>
         </div>
-        <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
-            <input type="checkbox" name="requires_lm_awb" value="true" style="width: auto;" {% if shipment.requires_lm_awb %}checked{% endif %}>
-            <label style="margin: 0; font-weight: 500;">Final delivery tracking pending</label>
+        <div style="display: flex; gap: 1rem;">
+            <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
+                <input type="checkbox" name="requires_lm_awb" value="true" style="width: auto;" {% if shipment.requires_lm_awb %}checked{% endif %}>
+                <label style="margin: 0; font-weight: 500;">Final delivery tracking pending</label>
+            </div>
+            <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
+                <input type="checkbox" name="custom_duty" value="true" style="width: auto;" {% if shipment.custom_duty %}checked{% endif %}>
+                <label style="margin: 0; font-weight: 500;">Custom duty</label>
+            </div>
         </div>
 
         <h3 style="border-bottom: 1px solid var(--border-color); padding-bottom: 0.5rem; margin-top: 2rem;">Money</h3>
@@ -231,7 +242,7 @@
     const receivedInput = document.getElementById('received_amount');
     const selfCostInput = document.getElementById('self_cost');
     const otherExpInput = document.getElementById('other_expense');
-    
+
     const balanceDisplay = document.getElementById('balance_display');
     const profitDisplay = document.getElementById('profit_display');
 
@@ -240,12 +251,12 @@
         const received = parseFloat(receivedInput.value) || 0;
         const selfCost = parseFloat(selfCostInput.value) || 0;
         const otherExp = parseFloat(otherExpInput.value) || 0;
-        
+
         balanceDisplay.textContent = (billed - received).toFixed(2);
-        
+
         const profit = billed - selfCost - otherExp;
         profitDisplay.textContent = profit.toFixed(2);
-        
+
         const profitContainer = profitDisplay.parentElement;
         if (profit > 0) {
             profitContainer.style.color = 'var(--success-text)';
@@ -265,7 +276,7 @@
         const form = current.form;
         if (!form) return;
         const elements = Array.from(form.elements).filter(el => !el.disabled && el.type !== 'hidden' && el.tabIndex !== -1);
-        
+
         if (direction === 'next') {
             const index = elements.indexOf(current);
             if (index > -1 && index < elements.length - 1) elements[index + 1].focus();
@@ -339,9 +350,9 @@
         }
 
         if (['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight'].includes(e.key)) {
-            if (e.target.tagName === 'TEXTAREA') return; 
-            if (e.target.tagName === 'SELECT') return; 
-            
+            if (e.target.tagName === 'TEXTAREA') return;
+            if (e.target.tagName === 'SELECT') return;
+
             if (e.key === 'ArrowLeft' || e.key === 'ArrowRight') {
                 let isAtBoundary = false;
                 try {
@@ -407,18 +418,37 @@
         if (basisDisplay) basisDisplay.value = basis === 'volumetric' ? 'Volumetric' : 'Actual';
     }
 
+    let billedManuallyEdited = false;
+    let selfCostManuallyEdited = false;
+
+    if (billedInput) {
+        billedInput.addEventListener('input', () => { billedManuallyEdited = true; updateCalculations(); });
+    }
+    if (selfCostInput) {
+        selfCostInput.addEventListener('input', () => { selfCostManuallyEdited = true; updateCalculations(); });
+    }
+
+    function extractRateJS(text) {
+        if (!text) return 0;
+        let match = text.toLowerCase().match(/(\d+(?:\.\d+)?)\s*(?:\/kg|per\s*kg|kg)/);
+        if (match) return parseFloat(match[1]);
+        let matchPlain = text.match(/^\s*(\d+(?:\.\d+)?)\s*$/);
+        if (matchPlain) return parseFloat(matchPlain[1]);
+        return 0;
+    }
+
     function updateRateAmounts() {
         const customerWeight = document.getElementById('customer_charged_weight');
         const customerRate = document.getElementById('customer_rate_text');
         const vendorWeight = document.getElementById('vendor_charged_weight');
         const vendorRate = document.getElementById('vendor_rate_text');
 
-        if (customerWeight && customerRate && billedInput && (customerWeight.value || customerRate.value)) {
-            const amount = (parseFloat(customerWeight.value) || 0) * (parseFloat(customerRate.value) || 0);
+        if (!billedManuallyEdited && customerWeight && customerRate && billedInput && (customerWeight.value || customerRate.value)) {
+            const amount = (parseFloat(customerWeight.value) || 0) * extractRateJS(customerRate.value);
             billedInput.value = amount ? amount.toFixed(2) : '0.00';
         }
-        if (vendorWeight && vendorRate && selfCostInput && (vendorWeight.value || vendorRate.value)) {
-            const amount = (parseFloat(vendorWeight.value) || 0) * (parseFloat(vendorRate.value) || 0);
+        if (!selfCostManuallyEdited && vendorWeight && vendorRate && selfCostInput && (vendorWeight.value || vendorRate.value)) {
+            const amount = (parseFloat(vendorWeight.value) || 0) * extractRateJS(vendorRate.value);
             selfCostInput.value = amount ? amount.toFixed(2) : '0.00';
         }
         updateCalculations();
@@ -441,7 +471,7 @@
     });
 
     updateWeightBasis();
-    updateRateAmounts();
+    updateCalculations();
     function normalizeCountryInput(field) {
         field.value = field.value.toUpperCase();
     }
diff --git a/app/templates/shipments/list.html b/app/templates/shipments/list.html
index 8b7a64c..f6c7345 100644
--- a/app/templates/shipments/list.html
+++ b/app/templates/shipments/list.html
@@ -28,9 +28,14 @@
                 <option value="booked" {% if status == 'booked' %}selected{% endif %}>Booked</option>
                 <option value="packed" {% if status == 'packed' %}selected{% endif %}>Packed</option>
                 <option value="bagged" {% if status == 'bagged' %}selected{% endif %}>Bagged</option>
+                <option value="in_scan" {% if status == 'in_scan' %}selected{% endif %}>In Scan</option>
+                <option value="bagging" {% if status == 'bagging' %}selected{% endif %}>Bagging</option>
                 <option value="received" {% if status == 'received' %}selected{% endif %}>Received</option>
                 <option value="in_transit" {% if status == 'in_transit' %}selected{% endif %}>In transit</option>
                 <option value="customs" {% if status == 'customs' %}selected{% endif %}>Customs</option>
+                <option value="hand_over_to_airline" {% if status == 'hand_over_to_airline' %}selected{% endif %}>Hand over to airline</option>
+                <option value="at_destination" {% if status == 'at_destination' %}selected{% endif %}>At destination</option>
+                <option value="custom_clearance" {% if status == 'custom_clearance' %}selected{% endif %}>Custom Clearance</option>
                 <option value="at_lm_partner" {% if status == 'at_lm_partner' %}selected{% endif %}>At LM partner</option>
                 <option value="out_for_delivery" {% if status == 'out_for_delivery' %}selected{% endif %}>Out for delivery</option>
                 <option value="undelivered" {% if status == 'undelivered' %}selected{% endif %}>Undelivered</option>
@@ -50,9 +55,26 @@
                 {% endfor %}
             </select>
         </div>
+        <div>
+            <label for="shipment-service">Service</label>
+            <select id="shipment-service" name="service">
+                <option value="">All services</option>
+                {% for option in service_options %}
+                <option value="{{ option }}" {% if service == option %}selected{% endif %}>{{ option }}</option>
+                {% endfor %}
+            </select>
+        </div>
+        <div>
+            <label for="shipment-custom-duty">Custom duty</label>
+            <select id="shipment-custom-duty" name="custom_duty">
+                <option value="">All</option>
+                <option value="yes" {% if custom_duty == 'yes' %}selected{% endif %}>Yes</option>
+                <option value="no" {% if custom_duty == 'no' %}selected{% endif %}>No</option>
+            </select>
+        </div>
         <div class="button-row">
             <button type="submit" class="btn btn-primary">Apply filter</button>
-            {% if q or status or country %}<a href="/shipments" class="btn btn-secondary">Clear</a>{% endif %}
+            {% if q or status or country or custom_duty or service %}<a href="/shipments" class="btn btn-secondary">Clear</a>{% endif %}
         </div>
     </form>
 </section>
@@ -70,8 +92,8 @@
             <thead>
                 <tr>
                     <th>Booking</th>
-                    <th>Customer</th>
-                    <th>Route</th>
+                    <th>Sender</th>
+                    <th>Receiver</th>
                     <th>Tracking</th>
                     <th>Status</th>
                     <th>Weight</th>
@@ -114,10 +136,21 @@
                         </div>
                     </td>
                     <td class="hover-preview-cell">
-                        <a href="/shipments/{{ s.id }}" class="row-open-link">
-                            <span class="cell-primary">{{ s.customer_name }}</span>
-                            <span class="cell-secondary">{{ s.customer_phone or s.contact_or_reference_raw or 'No contact' }}</span>
-                        </a>
+                        <div style="display: flex; flex-direction: column;">
+                            <a href="/shipments/{{ s.id }}" class="row-open-link" style="display: block;">
+                                <span class="cell-primary">{{ s.customer_name }}</span>
+                            </a>
+                            {% set wa_phone = s.customer_phone or s.contact_or_reference_raw %}
+                            {% if wa_phone %}
+                                {% set wa_url = main_tracking_url or main_tracking_site_url or '' %}
+                                {% set wa_message = "Here is your tracking link: " ~ wa_url ~ " and AWB: " ~ main_tracking_number %}
+                                <a href="#" class="cell-secondary whatsapp-app-link" data-whatsapp-phone="{{ wa_phone }}" data-whatsapp-message="{{ wa_message }}" style="text-decoration: underline; color: inherit; width: fit-content;" title="Open WhatsApp app">
+                                    {{ wa_phone }}
+                                </a>
+                            {% else %}
+                                <a href="/shipments/{{ s.id }}" class="row-open-link cell-secondary" style="display: block;">No contact</a>
+                            {% endif %}
+                        </div>
                         <div class="cell-hover-card">
                             <div class="hover-title">Raw items</div>
                             {% if preview.get('item_raw_text') %}
@@ -128,8 +161,8 @@
                         </div>
                     </td>
                     <td class="hover-preview-cell">
-                        <span class="cell-primary">{{ s.destination_country }}</span>
-                        <span class="cell-secondary">{{ s.destination_city or 'ΓÇö' }}</span>
+                        <span class="cell-primary">{{ s.receiver_name or 'ΓÇö' }}</span>
+                        <span class="cell-secondary">{{ s.destination_country }}</span>
                         <div class="cell-hover-card route-hover-card">
                             <div class="hover-title">Receiver address</div>
                             <div class="hover-line">{{ preview.get('address') or 'No address details added' }}</div>
@@ -139,9 +172,9 @@
                     <td class="tracking-cell">
                         <div class="tracking-stack">
                             <div class="tracking-line" data-tracking-type="main_awb">
-                                <a class="tracking-label-link" href="{{ main_tracking_url or main_tracking_site_url or '#' }}" target="_blank" rel="noopener" data-default-label="Main" data-track-label="Track Main" data-copy-number="{{ main_tracking_number }}" {% if main_tracking_site_url %}data-copy-mode="true"{% else %}data-copy-mode="false"{% endif %} {% if not main_tracking_url and not main_tracking_site_url %}hidden{% endif %}>Track Main</a><span class="tracking-label-text" {% if main_tracking_url or main_tracking_site_url %}hidden{% endif %}>Main</span>
-                                <input autocomplete="new-password" spellcheck="false" form="{{ row_form_id }}" class="mono row-edit-field" type="text" name="main_tracking_number" value="{{ main_tn.item.tracking_number if main_tn.item else '' }}" data-initial="{{ main_tn.item.tracking_number if main_tn.item else '' }}" placeholder="Main tracking">
-                                <select form="{{ row_form_id }}" class="row-edit-field" name="main_tracking_courier" data-initial="{{ main_courier or '' }}" aria-label="Main tracking courier">
+                                <a class="tracking-label-link" href="{{ main_tracking_url or main_tracking_site_url or '#' }}" target="_blank" rel="noopener" data-default-label="First Courier" data-track-label="Track First Courier" data-copy-number="{{ main_tracking_number }}" {% if main_tracking_site_url %}data-copy-mode="true"{% else %}data-copy-mode="false"{% endif %} {% if not main_tracking_url and not main_tracking_site_url %}hidden{% endif %}>Track First Courier</a><span class="tracking-label-text" {% if main_tracking_url or main_tracking_site_url %}hidden{% endif %}>First Courier</span>
+                                <input autocomplete="new-password" spellcheck="false" form="{{ row_form_id }}" class="mono row-edit-field" type="text" name="main_tracking_number" value="{{ main_tn.item.tracking_number if main_tn.item else '' }}" data-initial="{{ main_tn.item.tracking_number if main_tn.item else '' }}" placeholder="First Courier Number">
+                                <select form="{{ row_form_id }}" class="row-edit-field" name="main_tracking_courier" data-initial="{{ main_courier or '' }}" aria-label="First Courier Company">
                                     <option value="">Courier</option>
                                     {% if main_courier and main_courier not in courier_options %}<option value="{{ main_courier }}" selected>{{ main_courier }}</option>{% endif %}
                                     {% for courier in courier_options %}<option value="{{ courier }}" {% if courier == main_courier %}selected{% endif %}>{{ courier }}</option>{% endfor %}
@@ -169,9 +202,14 @@
                                 <option value="booked" {% if s.overall_status == 'booked' %}selected{% endif %}>Booked</option>
                                 <option value="packed" {% if s.overall_status == 'packed' %}selected{% endif %}>Packed</option>
                                 <option value="bagged" {% if s.overall_status == 'bagged' %}selected{% endif %}>Bagged</option>
+                                <option value="in_scan" {% if s.overall_status == 'in_scan' %}selected{% endif %}>In Scan</option>
+                                <option value="bagging" {% if s.overall_status == 'bagging' %}selected{% endif %}>Bagging</option>
                                 <option value="received" {% if s.overall_status == 'received' %}selected{% endif %}>Received</option>
                                 <option value="in_transit" {% if s.overall_status == 'in_transit' %}selected{% endif %}>In transit</option>
                                 <option value="customs" {% if s.overall_status == 'customs' %}selected{% endif %}>Customs</option>
+                                <option value="hand_over_to_airline" {% if s.overall_status == 'hand_over_to_airline' %}selected{% endif %}>Hand over to airline</option>
+                                <option value="at_destination" {% if s.overall_status == 'at_destination' %}selected{% endif %}>At destination</option>
+                                <option value="custom_clearance" {% if s.overall_status == 'custom_clearance' %}selected{% endif %}>Custom Clearance</option>
                                 <option value="at_lm_partner" {% if s.overall_status == 'at_lm_partner' %}selected{% endif %}>At LM partner</option>
                                 <option value="out_for_delivery" {% if s.overall_status == 'out_for_delivery' %}selected{% endif %}>Out for delivery</option>
                                 <option value="undelivered" {% if s.overall_status == 'undelivered' %}selected{% endif %}>Undelivered</option>
@@ -230,6 +268,41 @@
 {% block scripts %}
 <script>
 (() => {
+    function normalizeWhatsappPhone(phone) {
+        let digits = String(phone || '').replace(/\D/g, '');
+        if (digits.length === 10) digits = '91' + digits;
+        return digits;
+    }
+
+    function openWhatsAppApp(phone, message) {
+        const normalizedPhone = normalizeWhatsappPhone(phone);
+        if (!normalizedPhone) return false;
+        const encodedMessage = encodeURIComponent(message || '');
+        const appUrl = `whatsapp://send?phone=${normalizedPhone}&text=${encodedMessage}`;
+        const webUrl = `https://wa.me/${normalizedPhone}?text=${encodedMessage}`;
+        let fallbackTimer = null;
+        const cancelFallback = () => {
+            if (fallbackTimer) clearTimeout(fallbackTimer);
+            fallbackTimer = null;
+        };
+        window.addEventListener('blur', cancelFallback, { once: true });
+        document.addEventListener('visibilitychange', () => {
+            if (document.hidden) cancelFallback();
+        }, { once: true });
+        fallbackTimer = setTimeout(() => {
+            window.open(webUrl, '_blank', 'noopener');
+        }, 1200);
+        window.location.href = appUrl;
+        return true;
+    }
+
+    document.querySelectorAll('.whatsapp-app-link').forEach(link => {
+        link.addEventListener('click', event => {
+            event.preventDefault();
+            openWhatsAppApp(link.dataset.whatsappPhone, link.dataset.whatsappMessage || '');
+        });
+    });
+
     const filterForm = document.querySelector('.shipment-filter-form');
     if (filterForm) {
         const searchInput = document.getElementById('ledger-filter-input');
diff --git a/app/templates/shipments/new.html b/app/templates/shipments/new.html
index c010935..31927bb 100644
--- a/app/templates/shipments/new.html
+++ b/app/templates/shipments/new.html
@@ -17,7 +17,7 @@
 
 <div class="card form-card">
     <form method="POST" action="/shipments/new" class="shipment-form compact-shipment-form" id="new-shipment-form">
-        
+
         <div class="form-section-title">
             <h3>Sender & Receiver</h3>
         </div>
@@ -124,14 +124,14 @@
                 <input autocomplete="new-password" spellcheck="false" type="text" name="vendor_partner" placeholder="e.g. Nikhil, Self">
             </div>
             <div class="form-group">
-                <label>Main Tracking Courier <span class="required">*</span></label>
+                <label>First Courier Company <span class="required">*</span></label>
                 <select name="main_tracking_courier" id="main_tracking_courier" required>
                     <option value="">Select courier</option>
                     {% for courier in courier_options %}<option value="{{ courier }}">{{ courier }}</option>{% endfor %}
                 </select>
             </div>
             <div class="form-group">
-                <label>Main Tracking Number</label>
+                <label>First Courier Number</label>
                 <input autocomplete="new-password" spellcheck="false" type="text" name="main_tracking_number" placeholder="e.g. 1234567890">
             </div>
             <div class="form-group">
@@ -151,9 +151,14 @@
                     <option value="booked">Booked</option>
                     <option value="packed">Packed</option>
                     <option value="bagged">Bagged</option>
+                    <option value="in_scan">In Scan</option>
+                    <option value="bagging">Bagging</option>
                     <option value="received">Received</option>
                     <option value="in_transit">In Transit</option>
                     <option value="customs">Customs</option>
+                    <option value="hand_over_to_airline">Hand over to airline</option>
+                    <option value="at_destination">At destination</option>
+                    <option value="custom_clearance">Custom Clearance</option>
                     <option value="at_lm_partner">At LM Partner</option>
                     <option value="out_for_delivery">Out for Delivery</option>
                     <option value="undelivered">Undelivered</option>
@@ -175,9 +180,15 @@
                 <input autocomplete="new-password" spellcheck="false" type="text" name="status_raw_text" placeholder="e.g. Customer asked to hold, pending customs, follow-up needed">
             </div>
         </div>
-        <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
-            <input type="checkbox" name="requires_lm_awb" value="true" style="width: auto;">
-            <label style="margin: 0; font-weight: 500;">Final delivery tracking pending</label>
+        <div style="display: flex; gap: 1rem;">
+            <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
+                <input type="checkbox" name="requires_lm_awb" value="true" style="width: auto;">
+                <label style="margin: 0; font-weight: 500;">Final delivery tracking pending</label>
+            </div>
+            <div class="form-group" style="display: flex; align-items: center; gap: 0.5rem;">
+                <input type="checkbox" name="custom_duty" value="true" style="width: auto;">
+                <label style="margin: 0; font-weight: 500;">Custom duty</label>
+            </div>
         </div>
 
         <h3 style="border-bottom: 1px solid var(--border-color); padding-bottom: 0.5rem; margin-top: 2rem;">Money</h3>
@@ -226,7 +237,7 @@
     const receivedInput = document.getElementById('received_amount');
     const selfCostInput = document.getElementById('self_cost');
     const otherExpInput = document.getElementById('other_expense');
-    
+
     const balanceDisplay = document.getElementById('balance_display');
     const profitDisplay = document.getElementById('profit_display');
 
@@ -235,12 +246,12 @@
         const received = parseFloat(receivedInput.value) || 0;
         const selfCost = parseFloat(selfCostInput.value) || 0;
         const otherExp = parseFloat(otherExpInput.value) || 0;
-        
+
         balanceDisplay.textContent = (billed - received).toFixed(2);
-        
+
         const profit = billed - selfCost - otherExp;
         profitDisplay.textContent = profit.toFixed(2);
-        
+
         const profitContainer = profitDisplay.parentElement;
         if (profit > 0) {
             profitContainer.style.color = 'var(--success-text)';
@@ -260,7 +271,7 @@
         const form = current.form;
         if (!form) return;
         const elements = Array.from(form.elements).filter(el => !el.disabled && el.type !== 'hidden' && el.tabIndex !== -1);
-        
+
         if (direction === 'next') {
             const index = elements.indexOf(current);
             if (index > -1 && index < elements.length - 1) elements[index + 1].focus();
@@ -334,9 +345,9 @@
         }
 
         if (['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight'].includes(e.key)) {
-            if (e.target.tagName === 'TEXTAREA') return; 
-            if (e.target.tagName === 'SELECT') return; 
-            
+            if (e.target.tagName === 'TEXTAREA') return;
+            if (e.target.tagName === 'SELECT') return;
+
             if (e.key === 'ArrowLeft' || e.key === 'ArrowRight') {
                 let isAtBoundary = false;
                 try {
@@ -402,18 +413,37 @@
         if (basisDisplay) basisDisplay.value = basis === 'volumetric' ? 'Volumetric' : 'Actual';
     }
 
+    let billedManuallyEdited = false;
+    let selfCostManuallyEdited = false;
+
+    if (billedInput) {
+        billedInput.addEventListener('input', () => { billedManuallyEdited = true; updateCalculations(); });
+    }
+    if (selfCostInput) {
+        selfCostInput.addEventListener('input', () => { selfCostManuallyEdited = true; updateCalculations(); });
+    }
+
+    function extractRateJS(text) {
+        if (!text) return 0;
+        let match = text.toLowerCase().match(/(\d+(?:\.\d+)?)\s*(?:\/kg|per\s*kg|kg)/);
+        if (match) return parseFloat(match[1]);
+        let matchPlain = text.match(/^\s*(\d+(?:\.\d+)?)\s*$/);
+        if (matchPlain) return parseFloat(matchPlain[1]);
+        return 0;
+    }
+
     function updateRateAmounts() {
         const customerWeight = document.getElementById('customer_charged_weight');
         const customerRate = document.getElementById('customer_rate_text');
         const vendorWeight = document.getElementById('vendor_charged_weight');
         const vendorRate = document.getElementById('vendor_rate_text');
 
-        if (customerWeight && customerRate && billedInput && (customerWeight.value || customerRate.value)) {
-            const amount = (parseFloat(customerWeight.value) || 0) * (parseFloat(customerRate.value) || 0);
+        if (!billedManuallyEdited && customerWeight && customerRate && billedInput && (customerWeight.value || customerRate.value)) {
+            const amount = (parseFloat(customerWeight.value) || 0) * extractRateJS(customerRate.value);
             billedInput.value = amount ? amount.toFixed(2) : '0.00';
         }
-        if (vendorWeight && vendorRate && selfCostInput && (vendorWeight.value || vendorRate.value)) {
-            const amount = (parseFloat(vendorWeight.value) || 0) * (parseFloat(vendorRate.value) || 0);
+        if (!selfCostManuallyEdited && vendorWeight && vendorRate && selfCostInput && (vendorWeight.value || vendorRate.value)) {
+            const amount = (parseFloat(vendorWeight.value) || 0) * extractRateJS(vendorRate.value);
             selfCostInput.value = amount ? amount.toFixed(2) : '0.00';
         }
         updateCalculations();
@@ -436,7 +466,7 @@
     });
 
     updateWeightBasis();
-    updateRateAmounts();
+    updateCalculations();
     function normalizeCountryInput(field) {
         field.value = field.value.toUpperCase();
     }
diff --git a/app/templates/tracking/new_event.html b/app/templates/tracking/new_event.html
index 4f2ba3a..786f549 100644
--- a/app/templates/tracking/new_event.html
+++ b/app/templates/tracking/new_event.html
@@ -30,9 +30,14 @@
                 <option value="booked">Booked</option>
                 <option value="packed">Packed</option>
                 <option value="bagged">Bagged</option>
+                <option value="in_scan">In Scan</option>
+                <option value="bagging">Bagging</option>
                 <option value="received">Received</option>
                 <option value="in_transit" selected>In transit</option>
                 <option value="customs">Customs</option>
+                <option value="hand_over_to_airline">Hand over to airline</option>
+                <option value="at_destination">At destination</option>
+                <option value="custom_clearance">Custom Clearance</option>
                 <option value="at_lm_partner">At LM partner</option>
                 <option value="out_for_delivery">Out for delivery</option>
                 <option value="undelivered">Undelivered</option>
